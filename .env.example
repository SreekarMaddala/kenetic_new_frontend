# API Gateway HTTP API base URL, without a trailing slash.
VITE_API_BASE_URL=http://127.0.0.1:3001

# Values exported by the Cognito stack. Do not commit real credentials/config.
VITE_COGNITO_USER_POOL_ID=ap-south-1_example
VITE_COGNITO_CLIENT_ID=exampleclientid
